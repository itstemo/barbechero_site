---
/**
 * Spanish landing page, at `/` (astro.config.mjs: `defaultLocale: 'es'`,
 * `prefixDefaultLocale: false`). Everything lives in <LandingPage>, which
 * both locales share; this file only says which language it is.
 */
import LandingPage from '../components/LandingPage.astro';
---

<LandingPage locale="es" />
